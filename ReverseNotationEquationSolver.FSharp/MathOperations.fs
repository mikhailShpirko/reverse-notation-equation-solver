module MathOperations

let add (a: decimal, b: decimal) : decimal = 
    a + b

let subtract (a: decimal, b: decimal) : decimal = 
    a - b

let multiply (a: decimal, b: decimal) : decimal = 
    a * b

let divide (a: decimal, b: decimal) : decimal = 
    a / b